{
  "cli": {
    "version": ">= 5.9.1"
  },
  "build": {
    "preview": {
      "android": {
        "buildType": "apk"
      }
    },
    "preview2": {
      "android": {
        "gradleCommand": ":app:assembleRelease"
      }
    },
    "preview3": {
      "developmentClient": true
    },
    "development": {
      "developmentClient": true,
      "distribution": "internal"
    },
    "production": {
      "env": {
        // "API_KEY": "your_production_api_key",
        // "OTHER_SECRET": "your_other_production_secret"
      },
      "android": {
          "buildType": "apk"
        // "image": "latest" // Or specific image like "ubuntu-22.04-jdk-17-ndk-r25c"
                           // Expo often defaults to a suitable image for the SDK.
      }
      // "distribution": "store" // This is often implied for production,
                                // but can be made explicit if needed.
    }
  }
}